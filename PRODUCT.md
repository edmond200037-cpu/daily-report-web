# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要使用者是工地現場人員，主要在手機上快速填寫每日施工資料，也可能由現場主管檢視、整理與複製日報內容。

## Product Purpose

這是一個離線、手機優先的施工日報與水位變化工具。使用者可以在沒有帳號或後端服務的情況下，於瀏覽器中建立施工日報、管理水位量測資料、預覽報告並複製可交付的文字內容。成功標準是現場人員能可靠完成當日紀錄，且本機資料與歷史紀錄不因應用程式更新而遺失。

## Positioning

產品以瀏覽器本機 IndexedDB 作為資料保存邊界，不依賴帳號、後端或網路；同時提供施工工程條目、進料、水位量測與可複製日報輸出的整合工作流。

## Operating Context

使用情境是工地現場，以手機優先、可能離線的環境快速輸入資料。日報工作流包含工程條目、進料、聯絡事項、特殊事項、預覽與複製；水位工作流包含井位、量測、變化計算、匯入與最近資料保留。使用者可透過設定管理工地、工種、廠商、位置、材料類型與特殊模板等記憶資料，並使用備份與還原保存本機資料。

## Capabilities and Constraints

- 使用 Vite、TypeScript、IndexedDB、Hash Routing 與 Service Worker。
- 日報資料、草稿與歷史資料只保存在瀏覽器本機，不提供帳號或後端同步；定稿日報保留 7 個日曆日。
- 支援工程條目、施工工項、施工人數、進料、聯絡事項、特殊事項、預覽與文字複製。
- 支援普通進料與獨立進料；獨立進料可連接至最多一個工程條目，資料必須維持單一來源。
- 支援水位井位、量測、變化量計算、文字匯入與資料保留規則。
- 介面與內容使用繁體中文，採手機優先操作；輸入控制需支援行動裝置鍵盤與中文輸入法。
- IndexedDB 既有資料與歷史快照不可因設定重新命名、刪除或應用程式更新而被錯誤改寫或遺失。
- 需支援記憶主檔的 JSON 備份與合併還原；日報草稿、歷史與水位資料不納入備份。Service Worker 僅快取應用程式資源，不快取日報資料。
- Hash Routing 需支援日報與水位功能頁面，重新整理時不依賴伺服器重寫路由。

## Evidence on Hand

- 專案入口與主要 UI：`src/main.ts`
- 日報領域、控制器、驗證與格式化：`src/domain/daily.ts`、`src/daily/`、`src/data/daily-repository.ts`
- 水位模組：`src/water-level/`
- IndexedDB 與資料邊界：`src/db/`、`src/data/`
- PWA 資源：`manifest.webmanifest`、`service-worker.js`、`public/`
- 單元測試：`tests/unit/`
- 目前沒有外部帳號、後端 API、使用者見證或可引用的第三方產品證據；未來工作不得捏造這些內容。

## Product Principles

- 現場優先：讓使用者在手機與不穩定網路環境中快速完成紀錄。
- 資料可追溯：保留原始資料來源、歷史快照與備份還原能力。
- 單一來源：彙整、連接與輸出都應回到原始記錄，不建立容易分歧的複製資料。
- 可交付輸出：輸入資料必須能轉換成可預覽、複製與後續使用的日報文字。
- 定稿邊界：只有通過日報完整性檢查的草稿可定稿；定稿快照不可被後續設定回寫。
- 漸進式操作：先完成必要紀錄，再透過設定與記憶資料降低重複輸入成本。

## Accessibility & Inclusion

- 以手機觸控與現場快速操作為主要輸入方式。
- 所有互動控制需有清楚的繁體中文標籤，並維持鍵盤可操作性。
- 中文輸入法組字期間不得因重新渲染破壞輸入狀態；搜尋與表單需保留焦點及可預期的輸入行為。
- 錯誤、未完成與資料保存失敗狀態需明確告知，並盡量保留使用者已輸入內容。
