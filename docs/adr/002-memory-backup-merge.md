# ADR-002：記憶備份採合併去重

## 決策

記憶備份只涵蓋可重用主檔：工地、工種、工項、廠商、位置、材料類型、材料候選與特殊模板。匯入前驗證 `memories@2`，再以標準化名稱及父層工種關係合併；`memories@1` 仍可讀取，並將其所有資料視為正式主檔。

## 後果

- 已存在資料與使用次數不覆蓋；僅新增缺少記憶。
- 草稿、7 天定稿日報與水位資料永不在匯入交易中開啟。
- UI 必須顯示新增、略過與無效資料筆數。
- `memories@2` 保存候選／正式狀態；水位資料維持獨立，不能混入記憶備份。
