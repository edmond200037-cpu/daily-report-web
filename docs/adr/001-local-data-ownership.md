# ADR-001：本機資料所有權與定稿邊界

## 決策

產品維持 local-first：無帳號、無後端、無多人同步。`live_report_draft` 是唯一可編輯草稿；使用者選擇「定稿並複製」後，建立不可回寫的 `daily_reports` 快照，並建立新的當日草稿。

## 後果

- 定稿快照保留 7 個日曆日，期滿自動清除。
- 主檔記憶修改只影響後續草稿，不影響已定稿日報。
- PWA 更新只能更新 Cache Storage；更新前必須 flush 草稿，不能清除 IndexedDB。
