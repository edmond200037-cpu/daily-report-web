# 介面設計規格

## 狀態

已確認的目標設計；Phase 1 至 Phase 3.1 的日報骨架與工程資料列已實作，Bottom Sheet 等後續階段尚未實作。第一階段只涵蓋施工日報，不包含水位與設定中心。

## 設計目的

施工日報應呈現為現場人員熟悉的紙本工作表，而不是由大量圓角卡片、膠囊與裝飾標籤拼成的通用後台。使用者應能快速掃讀今天填了什麼、哪一筆仍待處理，以及下一個主要動作。

## 核心原則

1. **資料列優先**：重複資料以連續欄線與對齊文字呈現，不為每筆資料建立獨立浮動卡片。
2. **任務文字優先**：使用直接的繁體中文標題；移除不增加任務資訊的英文眉標。
3. **漸進揭露**：位置、備註、進料關聯與次要操作只在需要時展開。
4. **狀態輕量化**：以文字、細色線或小型標記表示狀態，不使用大型膠囊佔據內容層級。
5. **手機現場優先**：觸控目標至少 44px；窄螢幕不得產生水平捲動或因固定面板遮住主要工作。
6. **資料邊界不動**：視覺與 UI 展開狀態不得改變 IndexedDB、備份、formatter、定稿快照或 dirty 規則。

## 視覺語言

### 色彩

- 紙面：暖灰白，不使用純白大面積漂浮卡片。
- 主要文字：炭黑；次要文字：中性灰褐。
- 欄線：低對比灰；群組起點可使用較深欄線。
- 施工橘：只用於新增、主要操作、施工中或需注意狀態。
- 工程資料列的 `draft` 對外顯示為「未完成」；這只改變介面文字，不改變資料狀態名稱、驗證或保存行為。
- 未完成紅色只套用於右側狀態欄，採淡紅底、深紅文字與紅色細線；工種、代表工項、廠商與人數維持紙色底及黑灰文字。
- 成功、警告與危險色只承擔語意，不作裝飾背景。
- 所有文字與互動狀態在實作階段必須通過 WCAG AA 對比檢查。

### 形狀與層級

- 預設容器以欄線、段落與留白分組。
- 不使用漸層與裝飾陰影。
- 一般資料列不使用獨立圓角外框。
- 圓角保留給輸入控制、Bottom Sheet 與需要明確操作邊界的浮層。
- 主標題、資料內容、輔助說明與狀態必須形成穩定的四級文字層級。

## 日報資訊架構

維持既有資料分類與頁籤：

1. 工程
2. 進料
3. 聯絡
4. 特殊事項

基本資料、頁籤、資料清單與預覽定稿使用共用骨架。頁籤切換不改變資料所有權或儲存行為。

## 共用資料列

### 桌面

工程條目使用「工種、代表工項、廠商／人數、狀態」四欄；位置只在展開內容中呈現。代表工項取第一個非空白工項，若仍有其他非空白工項則附加「另 N 項」。進料、聯絡與特殊事項依各自資料調整欄位，但共用欄線、字級、狀態與展開模式。

Phase 4 是否擴及進料、聯絡與特殊事項，須先以完成後的工程列觀察新舊混用、掃讀一致性與操作負擔；沒有實際問題時不為形式一致而擴張修改。

### 手機

同一筆資料重排為三層：

1. 工種＋狀態
2. 代表工項＋「另 N 項」
3. 廠商＋人數＋展開提示

手機只是重新排列，不建立另一套資料結構或互動邏輯。

### 展開行為

- 點擊資料列在原位置展開既有編輯器。
- 展開、收合與切換輔助編輯器屬於暫時 UI 狀態，不造成 dirty。
- 拖曳、左滑刪除、鍵盤替代操作與 IME 行為必須保留。

## 進料關聯工作區

- 進料清單為主要內容。
- 完整關聯工具預設收合，透過「管理工程關聯」進入。
- 展開後保留 ADR-006 的來源、目標、連線、改接、解除與復原契約。
- 收合只是隱藏工具，不得清除選取以外的資料狀態，也不得改變任何連接。

## 預覽與定稿 Bottom Sheet

- 收合時只顯示精簡操作列。
- 展開時具有遮罩、標題、關閉按鈕、焦點圈限與 Escape 關閉能力。
- 手機限制最大高度，內容區獨立捲動；不得遮住後方內容後仍允許誤觸。
- 桌面使用較窄的底部或側向工作面板，不拉滿主要內容寬度。
- 空白或不可複製狀態不得顯示看似有效的複製操作。
- 開關、捲動與關閉 Bottom Sheet 不得更新草稿時間或觸發保存。

## 明確排除

- 不重做設定中心、水位頁或全站導覽。
- 不變更資料模型、IndexedDB schema、備份格式或 Service Worker。
- 不變更日報 formatter、驗證規則與定稿快照。
- 不移除任何既有日報功能。
